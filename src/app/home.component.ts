import { Component, OnInit } from '@angular/core';

@Component({
    selector: 'home',
    template: `
    <label>
        This is the home component
    </label>
    `
})
export class HomeComponent {
}